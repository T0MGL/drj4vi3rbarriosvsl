# ===================================
# VARIABLES DE ENTORNO - Dr. Javier Barrios VSL
# ===================================
# Este archivo es un ejemplo de las variables de entorno disponibles
# Copia este archivo a .env.local para desarrollo local
# En Vercel, configura estas variables en: Settings > Environment Variables

# -----------------------------
# META PIXEL (Facebook Ads)
# -----------------------------
# ID del Meta Pixel para tracking de conversiones
# Encuentra tu ID en: Meta Business Suite > Eventos > Conjuntos de datos > Detalles del píxel
# IMPORTANTE: El pixel YA está configurado en el código con el ID: 932741799179706
# Solo necesitas esta variable si quieres cambiar el ID según el entorno
NEXT_PUBLIC_META_PIXEL_ID=932741799179706

# -----------------------------
# GOOGLE ANALYTICS 4
# -----------------------------
# ID de medición de Google Analytics 4
# Formato: G-XXXXXXXXXX
# Encuentra tu ID en: Google Analytics > Admin > Flujos de datos > Tu sitio web
NEXT_PUBLIC_GA4_ID=G-XXXXXXXXXX

# -----------------------------
# GOOGLE TAG MANAGER
# -----------------------------
# ID de Google Tag Manager (YA ESTÁ CONFIGURADO: GTM-NP4M2976)
# Solo necesitas esta variable si quieres cambiar el ID
NEXT_PUBLIC_GTM_ID=GTM-NP4M2976

# -----------------------------
# GOOGLE SHEETS API (para guardar leads)
# -----------------------------
# URL del Google Apps Script para guardar los formularios
# Ver documentación en: services/sheetApi.ts
NEXT_PUBLIC_GOOGLE_SCRIPT_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec

# -----------------------------
# ENTORNO
# -----------------------------
# Opciones: development | production
NODE_ENV=production

# ===================================
# NOTAS IMPORTANTES
# ===================================
# 1. El Meta Pixel ya está ACTIVO en el código con el ID 932741799179706
# 2. Los eventos de conversión están optimizados para Meta Ads:
#    - PageView (automático al cargar)
#    - ViewContent (cuando reproducen el video)
#    - InitiateCheckout (cuando abren el formulario)
#    - Lead (cuando completan el formulario)
# 3. Para Vercel, NO necesitas el prefijo NEXT_PUBLIC_ en todas las variables,
#    pero es recomendable para variables que se exponen al cliente
# 4. NUNCA subas el archivo .env.local a Git (ya está en .gitignore)

# -----------------------------
# VERCEL API ROUTES (Backend)
# -----------------------------
# Variables SIN prefijo NEXT_PUBLIC_ (solo accesibles desde el servidor)
# CRÍTICO: Configurar en Vercel Dashboard > Settings > Environment Variables

# URL del Google Apps Script
GOOGLE_SCRIPT_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec

# Contraseña del CRM (para autenticación del panel)
CRM_PASSWORD=cambiar_en_produccion_password_secreto
